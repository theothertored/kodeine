/** A helper returning an ordinal suffix (st, nd, rd) for a given number. */
export declare const OrdinalSuffixHelper: {
    /** Returns an ordinal suffix (st, nd, rd, th), for the given number. */
    getSuffix: (number: number) => "st" | "nd" | "rd" | "th";
};
